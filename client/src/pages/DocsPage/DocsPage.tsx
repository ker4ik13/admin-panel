import s from "../GeneralPage.module.scss";

const DocsPage = () => {
  return (
    <div className={s.page}>
      <h2 className={s.title}>Документация</h2>
    </div>
  );
};

export default DocsPage;
